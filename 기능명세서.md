# 이음 기능 명세서

이 문서는 현재 `main` 브랜치와 운영 API를 기준으로 작성합니다.

## 핵심 기능

| 기능 | 설명 | 주요 API |
| --- | --- | --- |
| 인증 | 이메일·Google 로그인, 비밀번호 변경, 계정 삭제 | `/api/auth/*` |
| 사용자 프로필 | 직무, 회사, 언어, 시간대, 업무·점심시간, 커뮤니케이션 스타일 관리 | `/api/users/me` |
| 온보딩 | 신규 계정의 프로필·소통 방식·수신자·Gmail 설정 | `/api/users/me/onboarding` |
| 수신자 프로필 | 수신자 정보, 조직 관계, 선호 소통 스타일, 응답 지표 관리 | `/api/recipients/*` |
| 수신자 AI 분석 | 저장된 수신자 정보를 바탕으로 성향·용어·권장 소통 규칙 분석 | `/api/ai/recipients/analyze` |
| 메시지 메타데이터 | 초안의 우선순위·태그·핵심 용어·언어 분석 | `/api/ai/messages/metadata` |
| 메시지 최적화 | 발신자와 수신자의 직무·관계·언어·시간대·소통 스타일을 반영한 메시지 생성 | `/api/messages/optimize` |
| 메시지 발송 | 최종 수정본을 연결된 Gmail 계정으로 발송 | `/api/messages/send` |
| 임시 저장 | 작성 중인 메시지 저장·조회·삭제 | `/api/messages/drafts` |
| 받은 메시지 | Gmail 받은 편지함, 본문·첨부파일 조회, 일정 추출 | `/api/gmail/*` |
| 팀 일정 | 수동 일정과 이메일 기반 일정의 생성·수정·삭제 | `/api/team-memory/*` |
| 기록·대시보드 | 최적화·발송 기록과 사용자별 통계 조회 | `/api/history`, `/api/dashboard/summary` |
| 공지 관리 | 로그인 사용자의 공지 조회와 관리자의 작성·삭제 | `/api/notices` |

## 메시지 최적화 입력 기준

- 발신자: 직무, 기본 언어, 시간대, 업무시간, 선호 스타일
- 수신자: 직무, 국가, 언어, 시간대, 조직 관계, 응답 속도, 선호 소통 스타일
- 메시지: 제목, 본문, 목적
- 학습 맥락: 승인된 팀 메모리

원문의 날짜, 시간, 금액, 비율, 담당자와 고유명사는 임의로 변경하지 않습니다. 수신자별 결과는 별도 레코드로 저장하며, 사용자의 승인 없이 자동 발송하지 않습니다.

## 인증과 권한

- 대시보드와 온보딩은 유효한 JWT가 있어야 접근할 수 있습니다.
- 모든 사용자 데이터 API는 로그인 사용자 ID로 소유권을 제한합니다.
- 공지 작성·삭제 및 사용자 관리 API는 `users.admin=true`만 허용합니다.
- 신규 사용자의 관리자 값은 기본적으로 `false`입니다.

## 오류 처리

- 인증 없음 또는 만료: `401`
- 소유권 또는 관리자 권한 없음: `403`
- 존재하지 않는 데이터: `404`
- 잘못된 입력: `400`
- AI 공급자 오류: 공통 AI 오류 형식과 `429`, `502`, `503`, `504`

## 검증 명령

```bash
npm test
```
